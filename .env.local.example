# Supabase (legacy - will be removed after migration to D1)
NEXT_PUBLIC_SUPABASE_URL=REPLACE_ME
NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_ME
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=REPLACE_ME
STRIPE_SECRET_KEY=REPLACE_ME
STRIPE_WEBHOOK_SECRET=REPLACE_ME

# Stripe Price IDs
STRIPE_PRICE_BASIC=REPLACE_ME
STRIPE_PRICE_PRO=REPLACE_ME
STRIPE_PRICE_ENTERPRISE=REPLACE_ME

# App
NEXT_PUBLIC_APP_URL=REPLACE_ME
NEXT_PUBLIC_APP_NAME=REPLACE_ME

# Cloudflare D1
# D1 bindings are configured in wrangler.toml (no env vars needed for D1)
# For local dev with wrangler, D1 is accessed via bindings automatically
